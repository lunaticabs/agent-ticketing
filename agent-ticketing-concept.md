# Agent 票务系统 · 构想文档

> **工作标题**：在场（Presence）· 待定
> **场景**：ETHGlobal Tokyo 2026 · World「Best Use of World ID for Agents」赛道
> **日期**：2026-09-25

---

> ### ⚠️ 范围收缩说明（实现时更新）
>
> 本文档描述的是完整构想，其中**名额流转（转让）那一层最终没有实现**。实际交付的
> 系统只有 `locked` 一种模式：名额锁定在中签者身上，不可转让、不可代持、没有接收方
> 窗口，也没有按 continuity 的入账上限。相关章节（§4.1 #3、§4.2 缺口二、§5 洞察二、
> §6 的模式表、§7 的转让五规则、§9 的第 4/5 拍）已同步改写或删除。
>
> **为什么值得记下来**：砍掉流转层不只是少做一个功能——它抽掉了本文档原本的差异化
> 主张（"别人做谁能买，我们做买到之后怎么流转"）。保留下来的仍然是硬的东西：抽签
> 而不是先到先得、把闸门装在正确的操作上、continuity 标识贯穿审计。但论述重心从
> "流转层"移回了"队列本身"。

## 0. 一句话

> **让你的 agent 替你排队；名额到手的那一刻，要求真人当场授权——人不在，名额顺延给下一个真人。**

我们做的不是"给票务系统加个 World ID 登录"，而是**把「真人临场」做成排队系统里一个可随时调用的原语**。

但更准确的一句话是这一句：

> **别人做的是「谁能买到票」。我们做的是「票在谁手上、他此刻在不在」。**

---

## 1. 我们在做什么

一个给活动主办方用的排队 / 发票系统：

- 用户以**唯一真人**身份进入队列（防脚本、防一人多号）
- 用户的 **agent 代替人等待**，监控名额释放
- 名额释放时，agent 触发一次**新鲜验证**，真人在手机上确认
- 人在窗口内批准 → 成交；**人不在 → 名额自动顺延**
- 名额**锁定**在中签者身上，不可转让
- 主办方可以给 VIP 签发**有范围、会过期、可撤回**的次级授权

---

## 2. 为什么这是 Agents 赛道，而不是普通票务系统

| | 普通票务系统 | 我们 |
|---|---|---|
| World ID 的位置 | 入口处的登录闸门 | **流程中间的控制回路** |
| 谁在等待 | 人 | **agent** |
| 验证次数 | 1 次（注册时） | **N 次（每个关键时刻）** |
| 失败路径 | "你已经领过了"（状态检查） | **窗口过期 → 名额顺延（产品语义）** |

赛道的硬性要求第一条是：**必须接入 sandbox 上的官方 World ID for Agents**。纯队列系统用的是 IDKit 的 `action` + `nullifier`，**不满足资格要求**。把 agent 放进来，这个项目才落进这条赛道。

---

## 3. 生态位：我们站在谁的肩膀上

票务这个方向**已经有人做过**，而且做得不少。这一节把我们和他们的边界画清楚。

### 三层结构

| 层 | 回答的问题 | 谁在做 | 状态 |
|---|---|---|---|
| **分配层** | 谁**有资格**买 | **World Concert Kit**（官方产品） | 已上线，有真实商业巡演 |
| **购买层** | 把票**卖出去** | Eventick / Ticketo（2025 年获奖项目）+ 所有票务平台 | 被做过很多次 |
| **持有与流转层** | 名额到手之后、入场之前发生了什么 | —— | ⬅️ **空白，我们在这里** |

### Concert Kit 是什么，边界在哪

| 项 | 事实 |
|---|---|
| 产品 | **Concert Kit**（站点品牌 World Music，`music.world.org`），World 官方产品 |
| 定位 | "concerts are for real fans, not bots" |
| 上线 | 约 2026 年 4 月，已是生产产品 |
| 战绩 | "Humans Only Concert"（旧金山，4/17）—— World **声称**拦下 10 万+ 机器人请求，约 1,000 名已验证真人拿到票；**Thirty Seconds to Mars** 欧洲巡演提供 human-only 2-for-1 票 |
| 面向 | 艺人（"Are you an artist?" → music@toolsforhumanity.com） |

它的官方流程只有四步：

1. 下载 World ID App
2. 在 orb 完成真人验证
3. **CLAIM YOUR CODE** —— 解锁一个独家访问码
4. **GET YOUR TICKET** —— 把这个码拿到第三方票务平台上兑换

> **World 不持有票、不持有队列、不持有库存、也不持有转让权。它只持有「谁有资格拿码」这一件事。**

### 关键洞察：Concert Kit 的码是「无记名凭证」

它保护的是**"谁拿到码"**，不是**"谁最终坐在座位上"**。

第 3 步把码发出去之后，控制权就交给了两样东西：用户的自觉，和第三方平台的规则。而**黄牛真正赚钱的地方，恰恰不是"谁能买"，而是"买到之后怎么流转"**。

World 花了大力气守住入口，然后在第 3.5 步把控制权交了出去。

> **我们不做 Concert Kit 的竞品。我们补的是它第 3 步里缺的那个队列——并且不把
> 第 3.5 步的控制权交出去。**

### 已获奖项目的教训（这段是硬证据）

[Eventick](https://github.com/zanchary/ethglobalTaipei) 和 [Ticketo](https://github.com/ericwang520/ticketing-platform) 都拿了 **ETHGlobal Taipei 2025 的 World 奖池**。Eventick 尤其扎实：12 个合约、跨链桥（World Chain ↔ Celo）、离线 QR 验证、主办方身份验证。技术密度远高于一般的 hackathon 项目——**值得尊重**。

但它在防黄牛上留下了**可以用代码证明的缺口**。详见下一节。

---

## 4. 我们的增量 ⭐️

**这一节是整份文档的重点。**

### 4.1 相对 Concert Kit（分配层）

| # | 增量 | Concert Kit 的现状 | 我们怎么做 |
|---|---|---|---|
| 1 | **队列与抽签** | 公开流程里**没有队列、没有等待室**。拿码之后去第三方平台抢购，速度竞争发生在那里，World 管不到 | 把队列做进系统，并用**抽签**而不是先到先得——脚本的速度优势归零 |
| 2 | **持有期管理** | 码发出去即失控 | 从名额到入场全程可查；名额锁在中签者身上，不可转手 |
| 3 | **名额锁死** | **无**。码可自由转手 | 名额与中签者绑定，不可转让。Eventick 的 `transferFrom` 缺口在我们这里不存在，因为根本没有那条路径 |
| 4 | **委托授权** | **无** | VIP 的 scope 签发，带**过期与吊销**（接口保留，界面不展示） |
| 5 | **agent 在环** | **无**。全程要人操作 | agent 替人排队，在关键时刻把真人拉进来 |

### 4.2 相对已获奖的票务项目（购买层）

#### 缺口一：nullifier 被烧在了**错误的操作**上

Eventick 的 `WorldIDVerifier` 确实实现了 nullifier 防重：

```solidity
require(!nullifierHashes[nullifierHash], "Nullifier already used");
...
nullifierHashes[nullifierHash] = true;   // ← 在「验证」时烧掉
```

但它在**「验证」**这个动作上烧，而不是在**「购买」**上。然后看 `buyTicket()` 的全部检查：

```solidity
require(evt.isActive, "Event is not active");
require(evt.ticketsSold < evt.totalTickets, "No more tickets available");
require(msg.value == evt.ticketPrice, "Incorrect payment");
require(block.timestamp < evt.eventDate, "Event has already occurred");
```

就这四条。全文搜 `maxTicket | purchaseLimit | hasPurchased | onePerHuman | limitPer` —— **零命中**。

> **后果：一个已验证地址可以买光整场活动的票。**
> 它只需要"是已验证地址"（一个布尔值），而不是"这个人还没买过"。

而他们的项目描述里写着：*"ensures that a single identity can only purchase **limited tickets per event**, preventing mass purchases by scalpers"*。

**✅ 我们的增量：把 `action` 绑在「购买」这个操作上**（`buy_ticket_event_1`）。

因为 `nullifier = 用户 × app_id × action`——一人一票就自动成立了。**一个字段的选择，决定了整个防黄牛是否成立。**

#### 缺口二：票是**完全自由转让**的

Eventick 在全部 12 个合约里搜 `_transfer` / `transferFrom` / `soulbound` /
`nonTransferable` / `_beforeTokenTransfer` / `_update(` —— **零命中**。4 个合约继承
ERC721，没有任何转让限制。买票时那套 World ID 验证，一个 `transferFrom` 就绕过去了。

而两个缺口是**叠加**的：一个地址买光所有票，再 `transferFrom` 分发给任何人。

**✅ 我们的增量：名额根本不流转。**

构想阶段这里原本的方案是"转让过策略引擎"——`locked` / `gift` / `open` 三模式，
转让有摩擦、有留痕、有上限。实现时砍掉了，只留 `locked`。理由是：三模式里真正
防黄牛的只有 `locked`，`gift` 和 `open` 都是在"可转让"这个前提下做减损；而把前提
去掉更彻底，也少一整块需要证明的攻击面。

代价是真实的，不掩饰：**送票给朋友也不行**。低需求的活动本来可以放开转让，现在
没有这个旋钮。

#### 缺口三（共同的）：只有「地址」，没有「人」

两个项目的门禁都是 `isVerified(msg.sender)` ——**一个按地址的布尔值**。所以系统里**没有"同一个人"这个概念**，也就没有入账上限、没有留痕、没有跨账号追踪。

**✅ 我们的增量：continuity 标识。记录跟着人走——换账号、换设备、换新 agent 都洗不掉。**

### 4.3 完全没人做过的（全新）

- **agent 替人排队** + 名额到手时的**新鲜验证**
- **窗口过期 → 名额顺延**（失败路径本身就是产品语义）
- **三级权限结构**（主办方授权的 mentor / VIP）作为**可过期、可吊销的 scope**
- **sandbox 的 Human Continuity IdP 本身** —— Concert Kit 和两个获奖项目用的都是生产环境的旧栈，agent 那一层是**真空白**

### 4.4 一页总结

| | 别人 | 我们 |
|---|---|---|
| 分配 | ✅ Concert Kit 已做 | 复用 |
| 购买 | ✅ 多个项目已做 | 复用，但**闸门装在正确的操作上** |
| **队列 / 抽签** | ❌ | ✅ |
| **持有期** | ❌ | ✅ |
| **名额锁死** | ❌ | ✅ |
| **continuity 留痕** | ❌ | ✅ |
| **agent 在环** | ❌ | ✅ |
| **新鲜验证** | ❌ | ✅ |
| **委托授权** | ❌ | ✅ |

> **九个维度里，现有项目覆盖了前两个，我们覆盖其中五个**——而且重心不在覆盖面，
> 在"agent 可以合法替人买票"这一条：agent 排队、agent 请求授权、真人在手机上批准、
> 闸门放行。其余都是围绕这条主线的支撑。

---

## 5. 三个反直觉的结论 ⭐️

**这一节是支撑上面那些增量的推理。**

### 洞察一：唯一性是必要条件，不是充分条件

一人一个 World ID 能消灭"一个脚本买 500 张"。但它挡不住三种攻击：

- **速度套利** —— 黄牛只需要**一个**身份 + 更快的工具
- **雇佣真人** —— 50 个真人，每人一个真实 World ID，每人一张
- **二级市场** —— 见洞察二

> **⚠️ 这条直接改设计：队列必须是抽签，不能是先到先得。**
> 如果按到达顺序发号，唯一性形同虚设——速度优势会吃掉一切。
> 正确做法：**在时间窗内加入的人机会均等。**

### 洞察二：唯一性 ∩ 可转让 = 黄牛从「抢票者」变成「做市商」

一旦票可转让，黄牛**根本不需要打赢排队**。他只要挂个价："你的票我出 ¥3000。"

100 个中签者里总有人愿意卖。他不用雇人、不用脚本、不用抢——他做的是**做市**。

> **可转让性把唯一性刚关上的那扇门，从后面重新打开了。**

这条洞察原本导向"转让要过策略引擎"。实现时它导向了相反的结论：**既然可转让性
本身就是那道后门，那就不要这扇门**。系统只提供 `locked`。

这不是回避问题，是把取舍摆明：锁死之后，黄牛唯一剩下的手段是雇真人来排队——
成本最高、最难规模化的那一种。我们放弃的是灵活性，不是严谨性。

### 洞察三：新鲜验证不做墙，做税；牙齿在「留痕」

很多人以为"新鲜验证 = 要刷脸 = 要线下见面"。**两个都不对。**

- 新鲜验证发生在**接收方自己的设备**上，是对**服务**认证，不是对**发送方**认证
  → **异步、远程、两个人不需要见面**
- 走 sandbox 的 OIDC 路径时，新鲜度原语是 `auth_time`（RFC 9470 step-up）
  → **协议层面不需要摄像头**

那它到底做了什么？两件事：

1. **把一次授权变成一次真实的时间成本**
   名额到手时要求一个真人在一个**会过期的窗口**里响应。不可批量、不可预授权、
   不可代劳——人不在，名额就顺延给下一个人
2. **留下洗不掉的记录**
   每一次入队、中签、批准都挂在 continuity 标识上。换账号、换设备、换新 agent
   都洗不掉——40 个账号在系统里仍然只是 2 个人

> 窗口只让黄牛**变慢**；**continuity 的审计记录才让它有被抓住的风险**。
> 诚实地说：如果真要求线下见面，防黄牛会强得多——但事实不是。所以别在 demo 里吹过头。

---

## 6. 更深一层：这不是技术能解决的问题

**黄牛存在的原因是票定价低于市场价。**

一张票面 ¥1000、转手 ¥30000，那 ¥29000 的溢价就是一份**悬赏**。只要它存在，就一定有人去做这份工——用机器人、用真人、用内部关系都行。

**技术只能改变「谁拿到这 ¥29000」，不能消灭它。** 演出票务行业做了几十年，也没做出一套既能自由转让、又能防住黄牛的设计——因为那两件事在定义上互相抵消。

### 所以我们不宣称解决黄牛

**构想阶段**这里是一张三模式旋钮表（`locked` / `gift` / `open`），让主办方自己选
让黄牛付哪种成本。**实现时只保留了 `locked`**，理由在 §4.2 缺口二：`gift` 和 `open`
都是在"可转让"这个前提下做减损，而可转让性本身就是那道后门。

| 模式 | 转让 | 黄牛的主要攻击面 | 本实现 |
|---|---|---|---|
| `locked` | 不可转让，名额绑定人 | 只剩雇佣真人（成本最高） | ✅ 唯一实现 |
| `gift` | 可转让一次，接收方需新鲜验证 | 人海 + 有限倒卖 | ❌ 已移除 |
| `open` | 自由转让 | 二级市场完全打开 | ❌ 已移除 |

> **我们的立场：不消灭黄牛，而是选一种让黄牛付出最高成本的结构，并且不假装它没有代价。**
> 代价是送票给朋友也不行了。这是主动承担的取舍，不是漏做的功能。

---

## 7. 系统设计

### 状态机

```
入队(已验证) → 队列中 → [抽签] → 名额确认中 → 待真人批准 → 成交（锁定）
                                          │
                                          └─ 窗口过期 → 顺延给下一位
```

### World ID 调用点

| 时刻 | 要求 | 用到的原语 |
|---|---|---|
| 入队 | 持久证明（真人、唯一） | Proof of Human |
| **购买** | **`action` 绑在购买上** ← 关键 | `nullifier = 用户 × rp_id × action` |
| 名额到手 → 确认 | **新鲜验证**（窗口内 step-up） | `auth_time` + RFC 9470 / 短 TTL |
| 名额抽签 | 机会均等，与速度无关 | —— |
| 次级授权（VIP） | 签发有范围的 grant | sector + issuer/sub 绑定 + 可吊销 |

### 名额锁定

没有转让路径。名额一旦 `CONFIRMED`，就与中签者的 continuity 标识绑定到活动结束：

- 接口层面不存在转让端点
- 数据层面没有 `transfer` 表，也没有 `policy` 列可供配置
- `security-check` 会断言这些存储确实不存在——被删掉的功能如果把表留下了，
  它就会以意外的方式回来

### 次级权限（VIP）——实现为 API-only

> 界面上不出现角色相关的任何东西。这不是没做，是刻意不展示：本项目的主竞争点是
> **agent 合法代买**，VIP 徽章会把注意力从那条线拉走。

不做 `user.role = 'vip'`，而是**签发一个有范围的授权**给某个人：

- VIP = 队列优先 / 绕过抽签，但**带过期**
- 可随时吊销，且**特权也会衰减**——不是永久 god-mode

> mentor 的 scope 随转让一起移除了：它的语义是"可带 N 人入场"，而带人入场需要一条
> 把名额交出去的路径。没有转让，这个权限就没有任何机制可依托，留着只会是一个
> 授不出任何东西的按钮。

---

## 8. 诚实清单：我们挡不住什么

写在这里是因为：**想清楚了这些，才说明我们真的懂这个领域。**

- **高价活动的军团** —— 溢价足够大，雇真人就是划算的
- **胁迫与代持** —— 新鲜度证明"在场"，不证明"自愿"，更不证明"不是被雇的"。World ID 分不清粉丝和雇佣兵
- **摩擦落在正常用户头上** —— 送票给朋友也要约一个"活的时刻"。**这是主动承担的取舍，不是 bug**
- **内部勾结** —— 技术无关，靠流程审计

---

## 9. Demo 脚本（三分钟）

**场景用演唱会**——认知度最高、痛点最直观。但**开场白必须是这一句**，否则评委 5 秒内会归档成"又一个票务 / 又一个 Concert Kit"：

> "Concert Kit 证明了机器人可以被挡在门外，但它停在队列那儿。上一届拿 World 奖的票务项目，票甚至是自由转让的，一个 `transferFrom` 就绕过了它所有的验证。我们的选择更简单也更硬：名额根本不离开中签的那个人。剩下能攻的只有队列本身，这五拍讲的就是这个。"

| 拍 | 演什么 | 目的 |
|---|---|---|
| 1 | FCFS 队列被脚本碾压 → 切到抽签 → 速度优势归零 | 证明我们懂攻击面 |
| 2 | 名额到手 → agent 卡住 → 手机批准 → 成交 | 赛道要的 "at the moment" |
| 3 | 人不批准 → 倒计时归零 → **名额顺延** | 失败路径 = 产品功能 |
| 4 | **40 个账号加入队列 → 塌缩成 2 个 continuity 标识 → 只有 2 个排队位** | **⭐️ 全场核心** |
| 6 | 回放 / 改金额 / 换环境 → 三连拒 | 安全深度 = 差异化 |

**道具**：一块实时队列看板（顺延和拒绝必须肉眼可见）+ 一台手机。

> 第 5 拍的倒计时**不要剪掉**。那 15 秒就是我们的产品主张：
> 它对朋友是礼貌，对黄牛是成本。

---

## 10. 48 小时的取舍

**✅ 必做（demo 质量）**
- 真人入队 + 唯一性，**`action` 绑在购买上**
- agent 替排队 + 名额到时的新鲜验证
- 窗口过期 → 顺延
- 实时队列看板

**🟡 降级为 stretch**
- VIP：先做成一个 enum 字段 + 一条 scope 检查，**不做授权管理后台**

**❌ 明确不做**
- 出席签到。`present` 已经被"购买那一刻的新鲜验证"用掉了，再做签到是重复叙事

---

## 11. 待决问题（想听你的意见）

1. **抽签的粒度**：全场一次抽，还是分批多次抽？（影响公平感 vs 系统复杂度）
2. **`gift` 模式下"一次"的判定**：一个名额一生只能转一次，还是一个人一生只能接一次？
3. **顺延窗口多长**：90 秒还是 120 秒？太短正常用户赶不上，太长黄牛有空间
4. **VIP 是否需要"衰减"**：特权随时间降级，还是只保留过期与吊销？
5. ~~要不要做 `strict` 第四模式~~ —— 已由实现回答：只做 `locked`。

---

## 附录 A：赛道事实

- **赛事**：ETHGlobal Tokyo 2026（ETHTokyo Week，9/25–9/27，虎之门 Hills Forum）
- **赛道**：World「🤖 Best Use of World ID for Agents」—— **$7,500，最多 3 队各 $2,500**
- **5 条硬性资格要求**：
  1. 接入 sandbox 上官方 World ID for Agents（赛事提供的 dev 环境）
  2. 演示完整闭环：请求 → 用户完成 → 校验结果 → 受保护动作执行
  3. **演示失败路径**：拒绝 / 过期 / 取消时，受保护动作不发生
  4. 后端安全校验：不暴露 client secret，不把未校验的客户端响应当作授权
  5. 提交简短集成复盘（首次成功耗时、摩擦、缺失能力、最有价值的一个改进）
- **评委口味原文**："a meaningful action that needs a human identity or approval layer, **not simply a login screen added to an existing product**"
- **参考**：[赛道页](https://ethglobal.com/events/tokyo2026/prizes/world) · [sandbox 文档](https://sandbox.auth.world.org/docs) · [HITL 集成文档](https://docs.world.org/agents/human-in-the-loop/integrate.md)

## 附录 B：竞品与先例（全部可验证）

| 项目 | 性质 | 用的栈 | 结论 |
|---|---|---|---|
| [Concert Kit](https://music.world.org/) | **World 官方产品**，2026-04 上线 | 生产环境 IDKit / PoH | 分配层；码是无记名凭证，持有与流转失控 |
| [Eventick](https://github.com/zanchary/ethglobalTaipei) | ETHGlobal Taipei 2025 **World 奖池**，12 合约 + 跨链 + 离线 QR | MiniKit 3.0（已过时） | 购买层；**nullifier 烧错操作 + 票可自由转让** |
| [Ticketo](https://github.com/ericwang520/ticketing-platform) | ETHGlobal Taipei 2025 **World 奖池** | MiniKit 3.0，`VerificationLevel.Device`（已废弃） | 支付 demo；**无 tickets 表，验证状态存 localStorage** |

**两个技术栈警告**：三者用的都是**生产环境的旧栈**（MiniKit `verifyCloudProof` / `app_id` / 无 `rp_context`）。
**赛事要求的 sandbox Human Continuity IdP（sector / pairwise sub / fresh auth / grant 签发）没有任何一个项目用过——那是真空白。**
